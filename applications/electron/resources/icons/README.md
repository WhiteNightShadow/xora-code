# Platform icon assets

`1024x1024.png` is a packaging-safe placeholder generated from
`../branding/whitenight-code.svg`. Replace it with reviewed release artwork and
add platform-native assets before signing a public distribution:

- `icon.icns` for macOS
- `icon.ico` for Windows
- A complete PNG size set for Linux
